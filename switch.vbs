WScript.CreateObject("WScript.Shell").AppActivate(WScript.Arguments.Item(0))
'call this script and then give argument in double quotation markes
'eg. switch.vbs "Notepad"
'you can use variables to give arguments too like "%VariableName%"
